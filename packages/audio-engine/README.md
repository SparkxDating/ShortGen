# audio-engine

Future home for TTS and transcription providers.

Phase 1 does not replace Edge TTS, Azure Speech, Whisper, or the existing
subtitle pipeline. Those remain in `app/services/voice.py` and
`app/services/subtitle.py`.
