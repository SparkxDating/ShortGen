# media-engine

Future home for stock and local media providers.

Phase 1 does not replace `app/services/material.py` or `app/services/video.py`.
Visual source options in the SaaS UI map to the existing `pexels` / `local`
values on `VideoParams`.
