# Packages

Adapters around the existing MoneyPrinterTurbo engine.

- `shared` — queue, storage, security helpers
- `video-engine` — `MoneyPrinterTurboGenerationAdapter`
- `ai-engine` / `media-engine` / `audio-engine` — Phase 1 placeholders; the live implementations stay in `app/services/`
